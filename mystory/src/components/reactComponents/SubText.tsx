import React from 'react'
import styles from '../../styles/homepage.module.css'

export const SubText: React.FC = () => {
    return (
        <p className={styles.SubTitle}>{"HEHE"}</p>
    );
}

export default SubText;
