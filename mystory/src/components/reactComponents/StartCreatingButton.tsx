import React from 'react'
import styles from '../../styles/homepage.module.css'

export const StartCreatingButton: React.FC = () => {
    return (
        <button className={styles.StartCreatingButton}>{"Start Creating"}</button>
    );
}

export default StartCreatingButton;

